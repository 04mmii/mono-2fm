# Anthropic API 키 (서버리스 함수 api/recommend.js 에서만 사용)
# console.anthropic.com → Settings → API keys 에서 발급
#
# 주의: VITE_ 접두사를 붙이지 말 것. 붙이면 Vite가 클라이언트 번들에 그대로
# 넣어버려서 브라우저에서 누구나 키를 볼 수 있다.
ANTHROPIC_API_KEY=sk-ant-여기에-키
